services:
  api:
    build: ./pc-shop-api

    ports:
      - "3002:3002"

    env_file:
      - ./pc-shop-api/.env

    depends_on:
      - postgres
      - redis

  payment-service:
    build: ./payment-service

    depends_on:
      - redis

  postgres:
    image: postgres:16

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: pcshop

  redis:
    image: redis:7

    ports:
      - "6379:6379"
